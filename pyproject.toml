# pyproject.toml
[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest]
testpaths = ["tests"]

[tool.black]
line-length = 100
include = '\.pyi?$'

[tool.isort]
profile = "black"